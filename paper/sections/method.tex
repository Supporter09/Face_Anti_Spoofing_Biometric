\section{Method}
The planned production pipeline uses browser capture, server-side face localization and alignment, and a lightweight liveness classifier. The current scaffold uses a stub inference path while the trained model is being integrated.
